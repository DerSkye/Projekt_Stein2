// Projekt_stein2

Simple check for esp32 to check if a network can connect
Connects automatically and disconnects automatically if not longer available

Please consider:
For using in a new network the varaiables "ssid" and "password" have to be changed in code
